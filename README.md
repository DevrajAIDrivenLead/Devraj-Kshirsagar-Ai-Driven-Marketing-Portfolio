# Devraj Kshirsagar — AI-Driven Marketing Portfolio

This repository contains a simple static portfolio scaffold built with Tailwind CSS (via CDN). It was created automatically and uses placeholder content — replace the files with your own text, projects, and images.

## Files added

- `index.html` — main portfolio page (Tailwind CDN). Includes About, Projects, and Contact sections. Your LinkedIn link has been added.
- `assets/profile.svg` — placeholder profile image (SVG with initials).

## How to edit

- Open `index.html` and replace the placeholder text with your own bio, projects, and links.
- Add project screenshots or assets into the `assets/` folder and update the image paths in `index.html`.

## Deploy

This is a static site and can be hosted with GitHub Pages from the `main` branch (Settings → Pages → Source: "Deploy from a branch" → `main` / `/ (root)`).

## Next steps I can help with

- Replace placeholder content with data pulled from your LinkedIn profile.
- Add a contact form (serverless) or Netlify Forms integration.
- Convert to a React (Vite) app or add a GitHub Actions workflow for automatic deployment.

If you want me to populate the bio and projects from your LinkedIn profile, say "Use my LinkedIn content" and I will extract public profile text and draft content here.